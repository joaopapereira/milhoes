language: ruby
rvm: "2.2.0"

before_install:
  - rm -Rf /home/travis/.rvm/gems/ruby-2.2.0/cache/bundler/git/feedjira-*
  - ls /home/travis/.rvm/gems/ruby-2.2.0/cache/bundler/git/
  - gem install bundler

before_script:
  - bundle exec rake db:create:all
  - bundle exec rake db:migrate

script:
  - bundle exec rspec --format progress
